# 📊 Mini Polling Application

The **Mini Polling Application** is a full-stack web application that allows administrators to create polls and users to participate by voting on available options.  
The system displays **real-time voting results** and prevents **duplicate voting** using basic validation.

This project demonstrates **end-to-end full-stack development** including backend API design, frontend integration, and database management using a mock JSON database.

---

## 🚀 Features

- ✔ Create polls (Admin)
- ✔ List active polls
- ✔ Vote on polls
- ✔ Prevent duplicate voting (IP-based)
- ✔ View real-time poll results
- ✔ Responsive UI using Material UI
- ✔ RESTful API integration
- ✔ Modular backend and frontend architecture
- ✔ Error handling

---

## 🛠 Tech Stack

### 🔧 Backend
- **Runtime:** Node.js
- **Framework:** Express.js
- **Mock Database:** JSON Server
- **HTTP Client:** Axios
- **Data Format:** JSON

### 🎨 Frontend
- **Framework:** React.js
- **UI Library:** Material UI (MUI)
- **Routing:** React Router DOM
- **State Management:** React Hooks (`useState`, `useEffect`)
- **HTTP Client:** Axios

### 🧪 Tools
- Postman / Thunder Client (API Testing)
- Git & GitHub (Version Control)

---

## 🗄 Database Structure (`db.json`)

JSON Server is used as a mock database.

```json
{
  "polls": [
    {
      "id": 1,
      "question": "Which language do you prefer?",
      "isActive": true,
      "createdAt": "2026-02-09T10:15:30.000Z"
    }
  ],
  "options": [
    {
      "id": 1,
      "pollId": 1,
      "text": "JavaScript",
      "votes": 5
    }
  ],
  "votes": [
    {
      "id": 1,
      "pollId": 1,
      "optionId": 1,
      "userIp": "192.168.1.10"
    }
  ]
}

```
---

## 📌 Database Collections – Explanation

| Collection | Description |
|-----------|-------------|
| `polls` | Stores poll questions and active status |
| `options` | Stores options related to each poll |
| `votes` | Stores voting records for each poll |

> IDs are **auto-generated by JSON Server**.

---

## ⚙️ Backend Setup Steps

### 1️⃣ Navigate to Backend Folder
```bash
cd backend
```

### 2️⃣ Install Dependencies
```bash
npm install express cors axios json-server
```

### 3️⃣ Start JSON Server (Database)
```bash
npx json-server --watch db.json --port 3001
```

### 4️⃣ Start Express Server (API)
```bash
Open a new terminal:

node server.js
```

### 5️⃣ Verify Backend
```bash
Open browser:

http://localhost:5000/polls
```
If data appears, backend is running successfully.

## 💻 Frontend Setup Steps
### 1️⃣ Navigate to Frontend Folder
```bash
cd frontend
```
### 2️⃣ Install Dependencies
```bash
npm install
npm install axios react-router-dom @mui/material @emotion/react @emotion/styled
```
### 3️⃣ Start React Application
```bash
npm start
```
### 4️⃣ Open Application

Browser will open automatically at:
```bash
http://localhost:3000
```
### 🗄 How to Run JSON Server

JSON Server is used as the mock database.

Command
```bash
npx json-server --watch db.json --port 3001
```
## Output
Resources
```bash
http://localhost:3001/polls
http://localhost:3001/options
http://localhost:3001/votes
```
Purpose

Stores application data

Automatically generates IDs

Simulates REST API endpoints

---

## 🔗 API Endpoints

| Method | Endpoint | Description |
|-------|---------|-------------|
| GET | `/polls` | Get all active polls |
| GET | `/polls/:id` | Get poll details |
| POST | `/polls` | Create a new poll |
| POST | `/polls/:id/vote` | Vote on a poll |
| GET | `/polls/:id/results` | Get poll results |

---

## 📌 Sample API Requests

### ➕ Create Poll
```json
POST /polls
{
  "question": "Which framework do you prefer?",
  "options": ["React", "Angular", "Vue"]
}
```
### Vote on poll
POST /polls/1/vote
```json
{
  "optionId": 2,
  "userIp": "192.168.1.10"
}
```
---

## 📋 Assumptions Made

- User identity is tracked using IP address for basic duplicate vote prevention  
- Admin authentication is not implemented (trusted access assumed)  
- JSON Server is used instead of SQL/NoSQL database as per assignment requirements  
- Only one vote per user per poll is allowed  
- Polls are active by default after creation  
- No role-based access control is implemented  
- Application is intended for small to medium-scale usage  
- Network environment is trusted (local development setup)  

---

## ▶️ How to Run the Full Application

Open **three terminals**:

### 🟢 Terminal 1 – Database
```bash
cd backend
npx json-server --watch db.json --port 3001
```
### Terminal 2 – Backend API
```bash
cd backend
node server.js
```

### Terminal 3 – Frontend UI
```bash
cd frontend
npm start
```
### Then open:
```bash
http://localhost:3000
```

---

## 📁 Project Structure

```text
Mini-Polling-App/
├── backend/
│   ├── controllers/        # Request handling logic
│   ├── routes/             # API route definitions
│   ├── db.json             # Mock database (JSON Server)
│   └── server.js           # Express server entry point
│
└── frontend/
    ├── pages/              # Application pages/components
    ├── services/           # API service calls
    ├── App.js              # Root React component
    └── package.json        # Frontend dependencies
```
## ✅ Features Implemented

✔ Create polls (Admin)
✔ List active polls
✔ Vote on polls
✔ Prevent duplicate voting
✔ View real-time results
✔ Responsive UI using MUI
✔ Error handling
✔ Modular code structure

## 📧 Submission Details
```bash
GitHub Repository: https://github.com/Abhijeet-Gadakh/Mini-Pooling-application
```
